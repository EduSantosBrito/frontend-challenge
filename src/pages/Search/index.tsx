import { Link } from 'react-router-dom';

const Search = () => {
    return (
        <div>
            <Link to='/'>Home</Link>
            <Link to='/detail'>Detail</Link>
        </div>
    );
};

export default Search;
